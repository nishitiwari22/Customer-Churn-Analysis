# from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, roc_auc_score

# accuracy = accuracy_score(X_test, Y_pred)
# precision = precision_score(X_test, Y_pred)
# recall = recall_score(X_test, Y_pred)
# f1 = f1_score(X_test, Y_pred)
# roc_auc = roc_auc_score(X_test, Y_prob)

# print("Accuracy:", round(accuracy*100, 2))
# print("Precision:", round(precision*100, 2))
# print("Recall:", round(recall*100, 2))
# print("F1 Score:", round(f1*100, 2))

# print("ROC-AUC:", round(roc_auc, 2))  # usually not in %y_prob = model.predict_proba(X_test)[:,1]
